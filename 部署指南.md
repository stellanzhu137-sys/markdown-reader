# Markdown Reader 部署指南（无需 Mac）

> 适用环境：Windows 电脑 + iPad Pro / iPhone。整个流程不需要 Mac，不需要付费开发者账号。

## 流程总览

```
本文件夹（代码已生成完毕）
   │  ① 上传到 GitHub 公开仓库
   ▼
GitHub Actions（云端 Mac 自动构建，免费）
   │  ② 下载构建产物 MarkdownReader.ipa
   ▼
AltStore / SideStore（侧载工具，AltServer 有 Windows 版）
   │  ③ 用免费 Apple ID 签名安装
   ▼
iPad / iPhone 桌面出现 Markdown Reader ✓
```

---

## 第一步：上传代码到 GitHub

1. 注册/登录 [github.com](https://github.com)
2. 右上角 **+** → **New repository**：
   - 名字随意，例如 `markdown-reader`
   - **必须选 Public**（公开仓库才能免费使用 Mac 构建机；代码里没有任何隐私信息）
   - 点 **Create repository**
3. 在仓库页面点击 **uploading an existing file** 链接，然后把本文件夹中的以下内容**拖到网页里**（保持目录结构不变）：
   - `MarkdownReader/`（整个文件夹）
   - `.github/`（整个文件夹，注意名字以点开头）
4. 页面下方点 **Commit changes**

> **备选：用 git 命令行**（如果装了 git）：
> ```bash
> cd "C:\Users\Stell\Desktop\Project\Markdown Reader"
> git init
> git add .
> git commit -m "Initial commit"
> git branch -M main
> git remote add origin https://github.com/你的用户名/markdown-reader.git
> git push -u origin main
> ```

---

## 第二步：云端构建 IPA

1. 上传完成后构建**自动开始**：仓库页顶部 **Actions** 标签 → 能看到 "Build iOS IPA" 正在运行（黄点）
2. 等待 5–10 分钟：
   - ✅ **绿勾** = 成功，继续下一步
   - ❌ **红叉** = 失败 → 点进那次运行 → 点 "build" → 把红色报错日志**完整复制发给我**，我来修复
3. 成功后：点进那次运行记录 → 页面底部 **Artifacts** → 下载 `MarkdownReader-ipa`
4. 解压下载的 zip，得到 `MarkdownReader.ipa`，把它传到 iPad/iPhone（微信文件传输助手、iCloud Drive、邮件均可，存到"文件"App 里）

---

## 第三步：安装到设备（侧载）

### 方案 A：AltStore（推荐，步骤最少）

1. **Windows 上安装 iTunes 和 iCloud**（两个都必须装，AltServer 依赖它们的组件）：
   - iTunes（64 位）：https://www.apple.com/itunes/download/win64
   - iCloud（苹果官方 CDN 直链）：https://updates.cdn-apple.com/2020/windows/001-39935-20200911-1A70AA56-F448-11EA-8CC0-99D41950005E/iCloudSetup.exe
   - ⚠️ **都不能用微软商店版**（商店版缺少侧载所需组件；已装商店版请先卸载再装上面的版本）
   - iCloud 安装后**无需登录**，装好放着即可
2. 下载 **AltServer for Windows**：https://cdn.altstore.io/file/altstore/altinstaller.zip （解压后运行 setup 安装，官网页面：[altstore.io](https://altstore.io)）
3. 数据线连接 iPad/iPhone 到电脑，设备上点"信任此电脑"
4. 点 Windows 托盘区的 AltServer 菱形图标 → **Install AltStore** → 选择你的设备 → 输入 Apple ID 账号密码（直接提交给苹果服务器，用于生成免费签名）
5. 设备上：**设置 → 通用 → VPN与设备管理** → 点你的 Apple ID → **信任**
6. 打开设备上刚装好的 **AltStore** → 底部 **My Apps** → 左上角 **+** → 选择第三步传过去的 `MarkdownReader.ipa` → 再次输入 Apple ID → 等待安装完成，桌面出现 Markdown Reader 图标 🎉
7. **每 7 天签名到期前**：设备与电脑连同一 Wi-Fi，打开一次 AltStore 即可自动刷新（AltStore 也会在后台尝试自动刷新）

### 方案 B：SideStore（装好后完全脱离电脑，自动续期）

适合不想每周开电脑的场景。安装稍复杂：用 AltServer（按住 Shift 点托盘图标 → **Sideload .ipa**）把 SideStore.ipa 装进设备，配合 StosVPN 和配对文件即可在设备端自动刷新签名。详见官方文档 [sidestore.io](https://sidestore.io)。

> ⚠️ 免费 Apple ID 限制：最多同时侧载 3 个 App、每周最多注册 10 个 App ID。只装 Markdown Reader 完全够用。

---

## 日常使用

- **微信/飞书/Telegram 打开 .md**：点开文件 → 右上角 → "用其他应用打开" → 选 **Markdown Reader** → 自动复制进 App 并打开阅读
- **Files App 互传**：文件 App → 我的 iPhone/iPad → **Markdown Reader** 文件夹 ↔ App 首页文件列表互通（下拉可刷新）
- **新建/导入**：首页右上角 **+** → 新建文档 或 从文件 App 导入
- **删除**：首页列表左滑
- **阅读 ↔ 编辑**：打开文档后点右上角铅笔/眼睛图标切换；编辑自动保存
- **设置**：首页左上角齿轮（或文档内右上角齿轮）→ 默认视图模式 / 主题 / 字体大小

---

## 常见问题

| 问题 | 解决 |
|---|---|
| Actions 构建失败 | 把红色日志复制发给 Claude，远程修复后重新 push 即可 |
| "用其他应用打开"里没有 Markdown Reader | 确认 App 已安装；重启一次设备（系统缓存原因） |
| App 图标变灰 / 打不开 | 签名过期：打开 AltStore 刷新（方案 B 自动处理） |
| 想改功能/界面 | 告诉我需求 → 我改代码 → 你 push → 下载新 IPA → AltStore 里重新安装（覆盖更新，文档不丢） |

---

## 真机验收清单

- [ ] 启动 App → 首页文件列表，右上角 **+** 可新建 .md 文档
- [ ] 微信里打开一个 .md → 用其他应用打开 → Markdown Reader **直接打开并渲染**该文件
- [ ] Files App → 我的 iPhone/iPad → 出现 "Markdown Reader" 文件夹；拷入 .md 后 App 首页下拉刷新可见
- [ ] 阅读：标题 / 粗斜体 / 列表 / 引用 / 代码块 / 表格 / 链接正确渲染；`$E=mc^2$` 数学公式正常
- [ ] 编辑：等宽字体；改几个字 → 退出重开 → 修改已保存
- [ ] 设置：改字体大小 → 文档整体缩放；切深色主题 → 文档窗口立即跟随
